
# =============================================================================
# BURO STAAL DASHBOARD - ENVIRONMENT VARIABLES
# =============================================================================
# Kopieer dit bestand naar .env en vul de waarden in

# =============================================================================
# SECURITY - REQUIRED
# =============================================================================

# ============================================
# AUTH0 CONFIGURATION
# ============================================
# Get these from your Auth0 Dashboard (https://manage.auth0.com)
AUTH0_DOMAIN=your-tenant.eu.auth0.com
AUTH0_CLIENT_ID=your_client_id_here
AUTH0_CLIENT_SECRET=your_client_secret_here
AUTH0_AUDIENCE=  # Optional: your Auth0 API audience

# App configuration
APP_ORIGIN=http://localhost:3000  # Change to your production URL in production

# ============================================
# LEGACY PASSWORD AUTH (Optional - fallback)
# ============================================
DASHBOARD_PASSWORD=your_password_here

# ============================================
# SECRETS & TOKENS
# ============================================
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here
COOKIE_SECRET=your_cookie_secret_here  # Can use same as JWT_SECRET

# =============================================================================
# DATABASE - TURSO (REQUIRED)
# =============================================================================

# Turso Database URL (krijg je van Turso dashboard)
TURSO_DATABASE_URL=libsql://your-database-name.turso.io

# Turso Auth Token (krijg je van Turso dashboard)
TURSO_AUTH_TOKEN=your-turso-auth-token-here

# Hoe krijg je Turso credentials?
# 1. Ga naar https://turso.tech
# 2. Maak een gratis account aan
# 3. Maak een nieuwe database aan
# 4. Kopieer de TURSO_DATABASE_URL en TURSO_AUTH_TOKEN
# 5. Run: node setup-turso-db.mjs (om schema aan te maken)
# 6. Run: node seed-turso-db.mjs (optioneel: voor test data)

# =============================================================================
# OPTIONAL - INTEGRATIONS
# =============================================================================

# Slack Webhook (optioneel - voor notificaties bij nieuwe items)
# Krijg je van: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Webflow API (optioneel - indien je Webflow CMS gebruikt)
WEBFLOW_API_HOST=https://api-cdn.webflow.com/v2
WEBFLOW_SITE_API_TOKEN=your-webflow-site-token
WEBFLOW_CMS_SITE_API_TOKEN=your-webflow-cms-token

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# NETLIFY DEPLOYMENT:
# 1. Site settings → Environment variables
# 2. Add each REQUIRED variable above
# 3. Redeploy after adding/changing variables
#
# SECURITY CHECKLIST:
# ✅ JWT_SECRET is unique and strong (32+ characters)
# ✅ DASHBOARD_PASSWORD is not a common password
# ✅ TURSO credentials are correct
# ✅ Never commit .env to git (already in .gitignore)
# ✅ Never share tokens in documentation or commits
#
# GENERATE SECRETS:
# openssl rand -base64 32    # For JWT_SECRET
#
# =============================================================================
# LEGACY (NIET MEER GEBRUIKT)
# =============================================================================
# De applicatie is gemigreerd van Azure SQL naar Turso.
# Onderstaande variabelen zijn NIET meer nodig:
#
# AZURE_SQL_SERVER=
# AZURE_SQL_DATABASE=
# AZURE_SQL_USER=
# AZURE_SQL_PASSWORD=
# AZURE_SQL_PORT=1433
# ADMIN_SECRET=
#
# =============================================================================


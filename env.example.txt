# =============================================================================
# BURO STAAL DASHBOARD - ENVIRONMENT VARIABLES
# =============================================================================
# Kopieer dit bestand naar .env en vul de waarden in

# =============================================================================
# SECURITY - REQUIRED
# =============================================================================

# JWT Secret for authentication (REQUIRED in production)
# Generate: openssl rand -base64 32
# NEVER use the default value in production!
JWT_SECRET=your-generated-jwt-secret-here

# Dashboard Password (REQUIRED)
# This is the password users enter to login to the dashboard
DASHBOARD_PASSWORD=your-secure-dashboard-password

# Admin Secret for debug endpoints (REQUIRED for diagnostics)
# Generate: openssl rand -base64 32
# Only needed if you want to use /api/diagnostics, /api/test-auth, /api/test-db
ADMIN_SECRET=your-generated-admin-secret-here

# =============================================================================
# DATABASE - AZURE SQL (REQUIRED)
# =============================================================================

AZURE_SQL_SERVER=your-server.database.windows.net
AZURE_SQL_DATABASE=your-database-name
AZURE_SQL_USER=your-username
AZURE_SQL_PASSWORD=your-password
AZURE_SQL_PORT=1433

# Alternative: Complete connection string
# AZURE_SQL_CONNECTION_STRING=Server=your-server.database.windows.net,1433;Database=your-database;User Id=your-user;Password=your-password;Encrypt=true;TrustServerCertificate=false;

# =============================================================================
# OPTIONAL - AZURE FUNCTIONS
# =============================================================================

# Azure Functions API URL (optioneel - laat leeg voor directe database verbinding)
# Lokaal: http://localhost:7071/api
# Productie: https://buro-staal-api.azurewebsites.net/api
AZURE_FUNCTIONS_URL=

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# NETLIFY DEPLOYMENT:
# 1. Site settings → Environment variables
# 2. Add each variable above (except comments)
# 3. Redeploy after adding/changing variables
#
# SECURITY CHECKLIST:
# ✅ JWT_SECRET is unique and strong (32+ characters)
# ✅ DASHBOARD_PASSWORD is not a common password
# ✅ ADMIN_SECRET is set (for debug endpoints)
# ✅ Database credentials are correct
# ✅ Never commit .env to git (already in .gitignore)
#
# GENERATE SECRETS:
# openssl rand -base64 32    # For JWT_SECRET
# openssl rand -base64 32    # For ADMIN_SECRET
#
# =============================================================================
